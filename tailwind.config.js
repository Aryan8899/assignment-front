/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // This will search for all JavaScript, TypeScript, and JSX files inside all directories within src.
  ],
  darkMode: 'class', 
  theme: {
    extend: {},
  },
  plugins: [],
}
